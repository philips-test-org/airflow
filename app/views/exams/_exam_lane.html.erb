<div style="page-break-after: always">
  <h2><%= resourceName %></h2>
  <hr />
  <% orders.sort_by{|order| start_time(order)}.each do |order| %>
    <%= render partial: "order", locals: {order: order} %>
  <% end %>
</div>
